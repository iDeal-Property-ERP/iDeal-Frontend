services:
  web:
    build: .
    container_name: 'ideal-frontend-dev'
    ports:
      - '3008:3000'
    restart: unless-stopped
    env_file:
      - .env

networks:
  default:
    name: ideal-dev-net
    external: true
